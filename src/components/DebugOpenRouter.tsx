// Este componente foi removido na simplificação FASE 1
// Debug do OpenRouter não é necessário no sistema simplificado
// Problemas de API podem ser verificados no console do navegador

export default function RemovedDebugComponent() {
  return null;
}
